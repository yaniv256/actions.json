# Codex Adapter

This adapter will package the portable skill and MCP/runtime setup for Codex.

The core skill remains in `skills/core/`. Codex-specific metadata belongs in `skills/codex/agents/openai.yaml` and any future Codex packaging files.

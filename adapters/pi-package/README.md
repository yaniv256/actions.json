# Pi Package Adapter

This adapter will package the portable skill, MCP/runtime setup, and any Pi-specific extension glue.

Pi packages can bundle skills, extensions, prompts, and themes via a `pi` manifest in `package.json`.
